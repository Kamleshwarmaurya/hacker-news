import { ShortdomainPipe } from './shortdomain.pipe';

describe('ShortdomainPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortdomainPipe();
    expect(pipe).toBeTruthy();
  });
});
